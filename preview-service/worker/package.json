{
  "name": "@fmw/preview-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "types:check": "wrangler types --check",
    "test": "pnpm run types:check && vitest run",
    "deploy": "pnpm run test && wrangler deploy"
  },
  "dependencies": {
    "@cloudflare/containers": "^0.3.7"
  },
  "devDependencies": {
    "@cloudflare/vitest-plugin": "^1.1.8",
    "vitest": "^5.0.0",
    "wrangler": "^4.131.1"
  }
}
